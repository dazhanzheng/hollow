# hollow 白皮书

## 一种以语义为中心的个人文件操作系统

**版本**：Draft 1.0
**产品名**：**hollow**
**文档类型**：产品与技术愿景白皮书

---

## 摘要

过去几十年里，个人电脑的文件系统几乎没有改变其根本范式：文件被放在某个路径下，用户通过文件名、目录结构与时间记忆来组织和找回它们。这套体系在文件数量有限、信息类型单一、工作节奏较慢的时代尚可运转；但在今天，它已经越来越不适合真实的人类认知方式。

用户并不真正记得文件放在什么目录里。用户记得的是语义、上下文、用途、来源与关系。用户会记得“那份去年报税时用过的银行流水”“上周下载的那个签过字的合同”“有红色柱状图的截图”“某位同事发来的最新版方案”，却很难记得它们曾经被保存在什么具体路径之下。

**hollow** 的提出，正是为了重构这种失配。

hollow 不是一个传统意义上的文件夹工具，也不是一个更花哨的搜索栏。它是一个以 AI 驱动的、以语义为中心的个人文件摄取、理解、归档与检索系统。它为用户提供一个统一入口：任何下载、拖拽、保存得到的文件，都可以先进入 hollow 的入口区域。随后，系统自动理解文件内容、识别其语义、建立索引、建议命名和归档位置，并在需要时让用户通过自然语言快速找回它们。用户不再主要通过“路径”管理文件，而是通过“意义”管理文件。

在 hollow 的世界中，文件不再是孤立的字节对象，而是被理解的、可解释的、可检索的语义单元。
hollow 的目标不是取代底层文件系统，而是在其之上建立一个新的认知层：一个更接近人类记忆方式的文件操作界面。

---

# 一、问题背景：我们为什么仍然在用不适合人类的文件系统

## 1.1 路径式文件系统的历史合理性

传统文件系统的核心设计逻辑，是将文件组织在树形目录中。
这套设计非常适合计算机，因为它具有明确、可确定、可遍历的结构：

* 一个文件属于某个目录
* 一个目录位于某条路径上
* 用户通过命名和层级关系管理信息

这种设计的优点是确定性强、实现简单、兼容性高，并在几十年软件生态中形成稳定基础。

但它也有一个根本前提：
**用户必须记得“东西在哪里”。**

而这恰恰不是人类最擅长的事情。

## 1.2 人类记忆不是按路径组织的

人在寻找文件时，通常不会回忆精确路径，而会回忆如下信息：

* 文件大致是什么内容
* 文件和哪个项目、客户、事件有关
* 它大概是什么时间得到的
* 它来自谁、来自哪里
* 它长什么样
* 它与其他哪些文件有关联
* 它曾经被拿来做过什么

这是一种**语义记忆**、**情境记忆**、**关系记忆**与**任务记忆**的混合体，而不是路径记忆。

传统文件系统要求用户把自己的记忆方式翻译成计算机的路径方式，这种转换本身就带来持续摩擦。随着下载目录膨胀、聊天工具附件增多、截图与文档激增、项目并行与多设备协作普遍化，这种摩擦最终演变为长期混乱。

## 1.3 今天的文件管理痛点已经不是“存不下”，而是“找不到”

现代用户在文件管理上的主要问题，往往体现在以下几个方面：

### 目录失控

多数用户的下载目录与桌面最终都会成为混杂区。安装包、发票、合同、图片、截图、研究资料、压缩包、录音、聊天导出的文件混在一起，难以维护。

### 文件命名失效

文件名常常缺乏意义，例如：

* `final.pdf`
* `final_v2.pdf`
* `IMG_2038.PNG`
* `屏幕截图 2026-04-09 14.22.13.png`
* `合同(1)(2) - 副本.pdf`

命名没有形成稳定规范，后续检索能力极弱。

### 人工分类成本过高

“稍后再整理”是大多数文件混乱的起点。
因为手工归档需要做一系列决定：

* 这是哪一类文件？
* 应该放在哪个目录？
* 是否要重命名？
* 以后会不会和别的东西混在一起？
* 这个目录体系自己半年后还看得懂吗？

这些决策消耗精力，而绝大多数文件并不值得用户付出这么高的注意力成本。

### 搜索能力与真实需求不匹配

当前系统搜索通常依赖：

* 文件名
* 少量元数据
* 部分全文索引

但真实需求往往是：

* “找那份去年签过字的合同”
* “上周下载过一个有关向量数据库的 PDF”
* “那张截图里有二维码和付款成功字样”
* “报税时用过的全部银行流水”

这些需求具有明显的语义性、上下文性、多模态性和模糊性，现有搜索工具经常难以满足。

---

# 二、hollow 的核心主张

## 2.1 hollow 是什么

**hollow 是一个以 AI 驱动的文件摄取与检索系统。**

它的核心工作方式非常简单：

* 用户把文件扔进一个统一入口
* hollow 自动吸入、理解、标注、归档、索引
* 用户通过自然语言、结构化条件或视觉线索快速找回文件

它不是简单地“自动整理下载目录”，也不是“给 Finder/Explorer 加一个搜索框”。
它试图建立一个新的交互范式：

> **把文件放进去，AI 自动理解、归档、记住；需要时，你只要描述它。**

## 2.2 hollow 不取代底层文件系统，而是建立一层认知中间层

hollow 不需要发明一种全新的底层存储结构。它可以建立在现有文件系统之上，与 macOS、Windows、Linux 的目录体系共存。
它关注的不是磁盘如何存储，而是：

* 文件如何被摄取
* 文件如何被理解
* 文件如何被归类
* 文件如何被命名
* 文件如何被召回
* 文件之间如何形成关系

因此，hollow 是一个**语义层**，也是一个**认知层**。
它让用户面对的是一个“可理解的文件世界”，而不仅仅是一个“可遍历的路径世界”。

## 2.3 hollow 的哲学

hollow 建立在以下几个基本判断之上：

### 第一，文件系统应该适应人，而不是要求人适应文件系统

路径与目录是计算机友好的，不是人类友好的。
人的真实工作方式应该成为系统设计起点。

### 第二，搜索不是附属功能，而应成为主交互方式

对于大量用户而言，“找文件”比“摆放文件”更频繁、更重要。
因此，检索不是功能补充，而是整个系统的出口核心。

### 第三，自动化应该以信任为前提

AI 可以理解文件，但不能随意替用户决定一切。
hollow 必须提供可解释性、可回滚性、可纠正性和渐进自动化机制。

### 第四，文件不应被看作孤立对象

一张截图、一份合同、一封邮件附件、一张机票 PDF，往往共同构成某个任务或事件的一部分。
hollow 应该理解它们之间的关系，而不是只处理单文件分类。

### 第五，本地优先是基础信任条件

文件管理系统天然处理隐私数据、商业信息与个人档案。
因此，hollow 的第一性原则之一应当是本地优先、可控上传、清晰边界。

---

# 三、产品定义：hollow 的核心能力

## 3.1 统一入口

hollow 必须为用户提供一个明确、低摩擦、几乎零学习成本的入口。

### 入口形式一：入口文件夹

用户可指定一个目录作为 hollow 的摄取区，例如：

* `Hollow Inbox`
* `Drop Zone`
* `To Hollow`

任何通过下载、拖拽、复制得到的文件，都可以先进入这个目录。
系统监听此目录，自动开始处理流程。

### 入口形式二：桌面入口 UI

除了文件夹，hollow 还可以提供一个桌面浮窗、托盘组件或侧边吸附区：

* 文件可直接拖入
* 支持多文件批量投递
* 用户可在投递时附加一句话语义说明
* 可展示摄取状态、建议分类和最近动作

桌面入口的价值在于，它把“归档”从一个后台动作，变成一个轻量、可交互、可反馈的过程。

### 入口形式三：未来扩展入口

在长期形态中，hollow 的入口可进一步扩展到：

* 浏览器下载接管
* 邮件附件自动摄取
* 聊天软件接收的文件
* 手机扫描件同步入口
* 云盘同步目录
* 剪贴板或截图接入

但无论入口多少，理念保持不变：
**一切先进入 hollow 的理解层。**

---

## 3.2 智能摄取

当文件进入 hollow 后，系统不应立即“粗暴移动”，而应先完成摄取与理解。

### 3.2.1 文件基础识别

系统识别文件的基础属性，包括：

* 文件名
* 类型与扩展名
* 大小
* 创建与修改时间
* 来源路径
* 来源应用（若可获取）
* 哈希指纹
* 是否与历史文件重复或高度相似

这一步是后续一切能力的结构基础。

### 3.2.2 内容提取

不同文件类型对应不同解析能力：

* PDF：正文提取、版面识别、页级摘要
* Word：正文、标题、注释、元数据
* Excel：工作表名、表头、关键单元格语义、结构摘要
* PPT：标题、演讲页概念、图文描述
* 图片：OCR、视觉内容识别、场景特征
* 截图：界面元素、关键词、图表、二维码、表单
* 压缩包：目录结构、内部文件快速分析
* 音视频：转录、说话人切分、时间轴摘要
* 代码与文本：语言识别、项目语义、依赖特征

### 3.2.3 语义理解

这一步是 hollow 与传统文件整理工具的分水岭。

系统需要回答一系列问题：

* 这是什么？
* 这和什么有关？
* 它属于哪个主题、项目或场景？
* 未来用户最可能如何描述它？
* 它和已有文件是否构成一组？
* 它是否敏感？
* 它是否重要？
* 它是否是中间版本、最终版或副本？

这会生成丰富的文件语义画像，例如：

* 文件类别：合同 / 发票 / 报告 / 简历 / 研究论文 / 会议纪要 / 截图
* 主题标签：报税 / 日本签证 / 项目 Phoenix / 客户 Acme / 向量数据库
* 时间标签：2025Q4 / 2026 报税季 / 上周会议
* 实体标签：公司名 / 人名 / 地点 / 机构 / 项目名
* 操作状态：待处理 / 已归档 / 需确认 / 疑似重复
* 敏感等级：普通 / 隐私 / 财务 / 法务 / 高敏感

### 3.2.4 摘要与命名

hollow 应生成简洁、稳定、可读的摘要与建议命名。
例如将：

* `IMG_4288.PNG`
* `document(4).pdf`
* `final2.pptx`

转换为更接近用户认知的形式：

* `2026-03-18_日本签证补充材料截图.png`
* `Acme_续约合同_签署版.pdf`
* `Q2市场策略汇报_内部讨论版.pptx`

命名并不是装饰，而是提升整个系统外部可理解性的重要手段。

---

## 3.3 自动归档与保守自动化

hollow 的目标确实包括“自动把文件放到该去的地方”，但这个过程必须谨慎设计。

### 3.3.1 为什么不能一开始就全自动移动

自动分类一旦错，用户会立即失去信任。
文件管理的错误不是一次轻微失误，而是：

* 用户以为文件丢了
* 用户无法复原历史位置
* 用户开始怀疑整个系统
* 用户不再敢把重要文件交给它

因此，hollow 在产品策略上应采用渐进自动化：

### 3.3.2 渐进自动化的三个阶段

**阶段一：只索引，不移动**
文件保留原地或暂存于 Inbox，系统只做理解与建议。

**阶段二：建议归档，由用户确认**
系统给出建议目录与命名，用户一键通过。

**阶段三：高置信度自动归档**
仅对重复率高、模式稳定、用户已明确授权的类别自动执行。

### 3.3.3 归档规则的来源

归档决策应来自三种机制：

* 用户自定义规则
* 历史行为学习
* AI 语义推断

一个成熟的 hollow 应让这三者协同，而不是互相取代。

### 3.3.4 可逆性

所有移动、命名、归类动作都应被记录并可撤销。
hollow 应提供：

* 原路径记录
* 变更历史
* 一键回滚
* 最近操作时间线
* “恢复到摄取前状态”的能力

可逆性是自动化可信度的关键组成部分。

---

## 3.4 自然语言检索

hollow 的出口必须远比入口更强大。
因为对于用户而言，真正长期产生价值的，不只是“自动放好”，更是“以后一定找得到”。

### 3.4.1 检索方式

用户应能够通过多种形式找文件：

* 自然语言描述
* 条件筛选
* 关键词组合
* 时间与类型混合查询
* 视觉或场景特征描述

例如：

* “找去年报税时的所有银行流水”
* “上周下载的那个 AI 论文 PDF”
* “那张右上角有二维码的付款截图”
* “Acme 客户最新版合同”
* “我之前保存过一个关于向量数据库 benchmarking 的文档”

### 3.4.2 检索能力应由三层组成

**元数据检索**
时间、类型、大小、来源、标签、项目、目录等

**全文检索**
PDF 文本、OCR 结果、文档正文、表格文字等

**语义检索**
基于 embedding 与语义重排，按“意思”召回，而非只做精确匹配

### 3.4.3 查询理解

用户的查询通常并不标准，hollow 需要将其转化为机器可执行的检索计划：

例如
“找去年报税用过的银行流水”

可被理解为：

* 时间范围：去年报税季
* 文件类型：银行流水、账单、PDF、图片扫描件
* 标签：财务 / 报税 / 银行
* 检索方式：全文 + 语义
* 重排信号：与已有税务文件共现程度高的优先

### 3.4.4 结果展示必须可解释

hollow 返回结果时，不应只是“给出一堆文件名”。
它应告诉用户：

* 为什么这个文件匹配
* 是正文命中、OCR 命中还是语义相似
* 它与哪些文件相关
* 是否是重复文件
* 是否有更新版本
* 最近一次整理动作是什么

一个高质量的结果卡片，应该让用户在 2 秒内判断“是不是它”。

---

# 四、hollow 的交互范式

## 4.1 从“放置”转向“投递”

传统文件管理强调“把文件放到哪里”。
hollow 的第一步，是将用户行为改造为“把文件投递进系统”。

投递这个动作更符合现实，因为它减少了用户对最终目录结构的即时决策负担。
用户不需要在获得文件的那一刻思考长期存放位置。
用户只需要把它交给 hollow。

## 4.2 从“浏览目录”转向“描述目标”

传统找文件方式是：

1. 回忆可能目录
2. 点开、返回、再点开
3. 搜索几个关键词
4. 试错
5. 再尝试

hollow 希望将其转化为：

1. 用人类语言描述目标
2. 系统理解意图并召回最可能的候选
3. 用户快速确认

这是一次从“路径浏览”到“意图检索”的交互迁移。

## 4.3 从“文件对象”转向“任务集合”

长期来看，用户不一定总是在找单个文件。
用户常常是在找某件事相关的全部材料。

例如，“日本签证”这个语义集合，可能包含：

* 护照扫描件
* 行程单
* 机票
* 酒店预订单
* 财产证明
* 填写过的申请表
* 付款截图

hollow 应逐步支持这种任务集合视图，让用户看到的是“语义簇”而非散乱文件列表。

---

# 五、hollow 的系统架构愿景

## 5.1 总体结构

从系统视角看，hollow 可被理解为五层：

### 第一层：入口层

负责接收文件与用户输入，包括：

* 入口文件夹
* 拖拽 UI
* 下载接管
* 外部应用接入

### 第二层：摄取层

负责解析与结构化：

* 文件监听
* 类型识别
* 内容提取
* OCR / 转录
* 哈希去重
* 元数据标准化

### 第三层：理解层

负责语义建模：

* 文档摘要
* 分类与标签
* 命名建议
* 敏感性判断
* 关系推断
* 归档策略建议

### 第四层：存储与索引层

负责持久化与检索基础设施：

* 文件路径映射
* 元数据库
* 全文索引
* 向量索引
* 操作日志
* 用户规则库

### 第五层：交互层

负责对外提供能力：

* 搜索框
* 自然语言问答
* 文件卡片
* 时间线
* 关系视图
* 规则设置
* 撤销与审计界面

---

## 5.2 技术选型

hollow 采用**本地优先**的技术架构，大部分数据和处理在用户本机完成，仅将需要大模型的 AI 任务交给云端：

* **macOS 客户端（Swift/SwiftUI + hollow-core）**：Swift 6 + SwiftUI（Xcode 26）负责原生 macOS 用户界面与系统集成。hollow-core 是 Rust 编写的本地核心引擎，通过 FFI（C-compatible 静态库）链接进 Swift 应用，负责文件解析、索引构建、本地检索等不需要大模型的全部任务。Rust 的高性能与内存安全特性，确保本地处理大量文件时的效率与稳定性。
* **云端服务（hollow-server）**：Rust 编写的轻量 API 代理服务，部署于云端。仅负责本机无法高效完成的 AI 任务：调用 LLM API 生成摘要、分类、标签、命名建议，调用 Embedding API 生成向量（向量结果返回本地存储），以及高精度 OCR、音视频转录等重计算任务。

这一设计确保：

* **隐私安全**：文件内容和语义数据默认存储在本地，仅在需要 AI 处理时才向云端发送必要内容
* **离线可用**：断网时用户仍可浏览、搜索已索引的全部文件
* **低云端负载**：服务端仅做 API 转发，不存储用户文件和索引数据
* **低延迟**：搜索在本地完成，无需网络往返

五层架构在技术栈中的对应关系：

| 架构层 | 技术实现 | 运行位置 |
|--------|---------|---------|
| 入口层（Intake） | Swift/SwiftUI — 拖拽 UI、文件夹监听、系统集成 | 本地 |
| 摄取层（Ingestion） | hollow-core — 文件解析、正文提取、哈希去重；Swift — macOS Vision 基础 OCR | 本地 |
| 理解层（Understanding） | hollow-server — 调 LLM API 做摘要、分类、标签、命名、敏感判断、关系推断；hollow-core — 规则引擎 | AI 部分云端，规则部分本地 |
| 存储与索引层（Storage/Index） | hollow-core — SQLite 元数据库、全文索引、向量索引、操作日志 | 本地 |
| 交互层（Interaction） | Swift/SwiftUI — 搜索界面、文件卡片、时间线、设置；hollow-server — 查询理解、结果重排 | UI 本地，AI 辅助搜索云端 |

---

## 5.3 数据模型原则

hollow 不应只把文件记录为“路径 + 文件名 + 日期”。
它需要更丰富的数据模型。

一个文件对象至少应包含以下维度：

* 基本标识：ID、哈希、路径、原路径
* 文件属性：类型、大小、时间戳
* 文本内容：正文、OCR、转录、结构化字段
* 语义信息：标签、摘要、分类、实体
* 关系信息：所属主题、关联文件、版本关系、重复关系
* 行为信息：摄取时间、移动记录、用户纠正记录
* 检索信息：embedding、关键词倒排索引
* 安全信息：敏感等级、上传策略、访问控制

长期来看，这个数据模型甚至会比底层目录结构更重要，因为它决定了 hollow 能否成为一个真正“理解文件”的系统。

---

## 5.4 混合智能原则

hollow 不能把所有事情都交给一个大模型。
正确的架构应当是混合式：

* 规则系统保证稳定性
* 统计模型保证效率
* 向量检索保证语义召回
* 大模型负责高层理解、解释与交互

例如：

* 扩展名识别不需要 LLM
* 哈希去重不需要 LLM
* OCR 可以由专门模型完成
* 目录推荐可以由规则与模型共同决定
* 查询解释与结果重排更适合 LLM

这能在成本、速度、精度与可控性之间取得更好平衡。

---

# 六、产品设计原则

## 6.1 用户永远可以知道系统做了什么

任何自动动作必须可见、可解释、可追踪。

## 6.2 自动化应先节省时间，再增加安心感

快不够，必须让用户敢用。

## 6.3 默认保守，逐步学习

先从建议开始，再过渡到高置信度自动执行。

## 6.4 搜索优先于目录

目录可以保留，但不应再是主要交互中心。

## 6.5 本地优先，云端增强

除非用户明确允许，否则敏感文件不应被默认外传。

## 6.6 系统必须允许被教会

用户纠正一次，系统就应在未来更接近用户习惯。

## 6.7 结果必须可解释

“为什么找到它”比“找到它”本身更重要。

---

# 七、典型使用场景

## 7.1 长期失控的下载目录

用户每天下载十几到几十个文件，几周之后下载目录完全失去秩序。
使用 hollow 后，所有下载文件先进入统一入口，由系统自动识别类型与主题，形成可搜索的归档流。

## 7.2 项目型知识工作者

产品经理、研究员、设计师、顾问、律师、财务人员，会围绕某个项目积累大量异构材料。
hollow 可将这些材料按语义关系与时间线聚合，而非要求用户预先手动分类。

## 7.3 高频截图用户

许多重要信息以截图形式存在。传统系统几乎无法管理这些内容。
hollow 可通过 OCR 与视觉理解，让截图成为可检索资产，而非信息黑洞。

## 7.4 个人财务与法务整理

合同、账单、发票、报税材料、银行流水、保险文件，往往具有高价值、高敏感、高重复调用频率。
hollow 可通过稳定命名、敏感标记与任务聚合，大幅降低查找成本。

## 7.5 旅行、签证、搬家、购房等事件型整理

这些活动通常涉及多个来源、多种格式、跨数周乃至数月的材料收集。
hollow 的事件聚合能力有潜力将这些分散文件变成清晰的行动包。

---

# 八、风险、边界与挑战

## 8.1 错误归类风险

如果自动归档错误，用户会迅速丧失信任。
解决方式不是承诺“绝不出错”，而是：

* 高置信度阈值
* 先建议、后自动
* 可解释与可撤销
* 持续学习

## 8.2 隐私与安全风险

文件系统中包含高度敏感数据。
hollow 必须允许用户定义：

* 哪些目录被扫描
* 哪些目录被忽略
* 哪些文件类型不上传
* 是否仅使用本地模型
* 是否对高敏感文件禁用云端增强

## 8.3 用户目录逻辑高度个性化

不同用户有不同的组织偏好：

* 按项目
* 按年份
* 按客户
* 按文件类型
* 按用途
* 按“以后可能怎么找”

hollow 不能预设唯一正确的目录哲学。
它需要允许多种策略共存，并随着使用逐渐适配。

## 8.4 模糊查询与错误召回

自然语言检索带来便利，也带来不确定性。
系统必须在模糊性与可用性之间平衡，既要理解“那个东西”，也要避免过度自信。

## 8.5 多模态成本与性能

OCR、视觉理解、嵌入计算、重排推理都可能消耗大量资源。
因此，系统需要分层处理、异步索引、缓存策略和优先级队列。

## 8.6 产品教育问题

用户长期习惯于“桌面 + 下载目录 + 文件夹”模式。
hollow 若想改变习惯，就必须在入口极简、收益立竿见影、结果可见可控这三方面做到足够强。

---

# 九、hollow 的阶段性路线图

## 9.1 第一阶段：可用的语义入口

目标不是“全自动文件操作系统”，而是先做出一个真正有价值的入口层产品。

核心能力：

* 监听统一入口文件夹
* 支持拖拽摄取
* 解析常见文档与图片
* 自动摘要与标签
* 建立全文与语义索引
* 提供自然语言搜索
* 推荐归档位置，但不强制自动移动

这一阶段的成功标志是：
用户即使不改变现有文件系统习惯，也愿意先把文件交给 hollow。

## 9.2 第二阶段：可信的自动归档

当系统积累了足够多的用户纠正与使用历史后，可引入自动归档：

* 命名建议
* 目录建议
* 高置信度自动移动
* 用户规则编辑器
* 操作回滚中心
* 重复与版本识别

这一阶段的成功标志是：
用户开始把 hollow 视为自己文件世界的主要入口，而不仅是一个搜索工具。

## 9.3 第三阶段：从文件管理到任务管理

下一步，hollow 可从“找文件”提升为“组织一件事所需的所有材料”：

* 事件聚合
* 项目空间
* 文件间关系图谱
* 时间线视图
* 主动建议与提醒
* 跨来源材料整合

这一阶段的成功标志是：
用户不再只问“文件在哪”，而是开始问“和这件事相关的东西都有哪些”。

## 9.4 第四阶段：个人知识与资产操作系统

更长远地看，hollow 有机会成为个人数字资产的统一语义层，不只涵盖本地文件，还包括：

* 云盘文档
* 邮件附件
* 笔记系统
* 扫描件
* 会议录音
* 网页剪藏
* 设计素材
* 合同和票据
* 项目资料库

届时，hollow 的价值将不止于文件整理，而是成为一个个人信息世界的语义操作系统。

---

# 十、hollow 的长期意义

## 10.1 从“储存信息”到“理解信息”

文件系统诞生时，计算机的主要任务是保存与读取。
今天，计算机的能力已经扩展到理解、归纳、总结、预测、联想。
因此，文件管理系统也应从被动储存走向主动理解。

## 10.2 从“人工秩序”到“机器协助秩序”

过去，秩序完全依赖用户建立。
未来，秩序应由机器协助生成，再由用户轻量校正。
这不是剥夺控制权，而是把用户从低价值的重复劳动中解放出来。

## 10.3 从“路径世界”到“语义世界”

路径不会消失，但路径不应再是用户与文件世界交互的主入口。
真正符合未来趋势的方式，是让用户直接与意义、任务、关系和上下文交互。

---

# 十一、结语

hollow 的出发点非常朴素：
人类并不擅长管理路径，却一直被迫通过路径管理自己的数字生活。

在传统系统中，文件被要求“待在某个地方”；
在 hollow 中，文件首先被要求“被理解”。

这一区别看似细微，实则意味着整个文件管理范式的变化。
它把文件从静态对象转化为可被解释、可被召回、可被组织进语义网络中的数字资产。
它让“我把它放哪了”这个问题，逐步让位于“它是什么”“它和什么有关”“我现在需要哪个”。

如果说过去的文件系统是为计算机设计的秩序，那么 hollow 所尝试建立的，是一种更接近人类思维方式的秩序。
在这样的秩序中，入口统一、理解自动、归档渐进、检索自然、关系可见、历史可追。
用户不再被迫成为自己信息世界的管理员，而是终于拥有一个真正能协助自己的系统。

**hollow 的愿景，不是整理文件。**
**hollow 的愿景，是重写人与文件之间的关系。**
